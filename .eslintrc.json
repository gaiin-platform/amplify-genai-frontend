{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-console": "warn",
    "no-restricted-imports": ["warn", {
      "paths": [{
        "name": "styled-components",
        "message": "styled-components is frozen. Use Tailwind CSS classes instead."
      }]
    }]
  }
}
